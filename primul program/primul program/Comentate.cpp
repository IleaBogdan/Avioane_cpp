//for (int k = 0; k < numar_puncte_x_dorite; k++) {
    //    int nrx;
    //    nrx = rand() % (m * n);
    //    numar_zar = numar_zar + 1;
    //    int i = nrx / n;
    //    int j = nrx % n;
    //    if (S[i][j] == '.') {
    //        S[i][j] = 'x';
    //        numar_puncte_x_existente = numar_puncte_x_existente + 1;
    //    }
    //    else
    //        k = k - 1;
    //}
    //cout << "\n" << numar_puncte_x_existente << "\n\n";

   /* if (numar_puncte_x_dorite > m * n) {
        cout << "Nu se pot inlocui punctele!";
        cout << "\n\n\n";
        return 1;
    }

    while (numar_puncte_x_existente<numar_puncte_x_dorite)
    {
        int nrx;
        nrx = rand()%(m*n);
        numar_zar = numar_zar + 1;
        int i = nrx / n;
        int j = nrx % n;
        if (S[i][j] == '.') {
            S[i][j] = 'x';
            numar_puncte_x_existente = numar_puncte_x_existente + 1;
        }
    }
    cout <<"\n"<<numar_zar<<"\n\n";*/
    //int numar_puncte_x_dorite;
    //int numar_puncte_x_existente;

    // cout << "Introduceti numarul de x-uri dorite:";
     //cin >> numar_puncte_x_dorite;
     //numar_puncte_x_existente = 0;
     //int numar_zar=0;

  /*int xx = 1;
    int yy = 1;
    int dir = 1;

    H[x][y] = 'x';
    H[x + (1 * xx) * dir][y - (1 * yy) * (dir-1)] = '0';*/

